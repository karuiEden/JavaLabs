- `Objects.requireNonNull(obj)` — обязательная проверка null, работает всегда. Для аргументов методов и публичного API.
- `assert obj != null` — проверка внутреннего предположения, используется при отладке. По умолчанию отключена (-ea для включения).